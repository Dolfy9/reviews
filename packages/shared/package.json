{
  "name": "@product-reviews/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Zod schemas and TypeScript types shared across apps",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
